# Flow Android — первый прототип

Самостоятельное нативное приложение на Kotlin. Next.js и сервер не используются.
yt-dlp, Python и FFmpeg поставляются Android-библиотекой youtubedl-android.

## Сборка

Откройте эту папку как проект в Android Studio. Требуются JDK 17 и Android SDK 35.
Gradle Wrapper 8.11.1 включён, архив проверяется по SHA-256.
Выполните `./gradlew assembleDebug` (Windows: `gradlew.bat assembleDebug`).
Для настроенной локальной среды Windows из корня репозитория:
`./scripts/build-android.ps1`. Инструменты хранятся в игнорируемой `.tools/android`.
APK: `app/build/outputs/apk/debug/app-debug.apk`.

## Проверка на телефоне

Android 8+, ARM64 / ARMv7 или эмулятор x86_64. Вставьте ссылку, нажмите
«Показать варианты», выберите качество, скачайте и сохраните через системный диалог.
Переключатель RU/EN запоминает язык. Режим аудио предлагает исходный звук и MP3.
При проблемах извлечения есть отдельная кнопка обновления yt-dlp.

## Границы прототипа

- Загрузка выполняется foreground-службой с уведомлением и отменой. Экран можно выключить.
- После готовности нажмите уведомление и сохраните файл через системный диалог.
- Принудительная остановка приложения, перезагрузка телефона или завершение процесса системой
  не восстанавливают задачу. Действуют ограничения Android и энергосбережения производителя.
- Один файл за раз; защитный тайм-аут — час. Подготовка списка форматов остаётся задачей экрана.
- Объединённое видео сохраняется в MKV для совместимости разных кодеков.
- Нет выдуманных разрешений: список строится из ответа источника, максимум 2160p.
- Временный файл хранится в кеше приложения; следующая загрузка удаляет предыдущий.
  Сохранённая через системный диалог копия не удаляется.
- Для публичного выпуска нужны проверка лицензий зависимостей, иконка,
  release-подпись, тесты хранения/сети и устройства с размером страницы 16 KB.

Библиотека: https://github.com/yausername/youtubedl-android

## Версия 0.3.0

- YouTube → «Поделиться» → Flow вставляет проверенную ссылку (загрузка не запускается автоматически).
- Новая ссылка во время работы не прерывает текущую задачу.
- Фоновая загрузка, RU/EN уведомления с прогрессом, отмена и возвращение к готовому файлу.
- Android 13+ запрашивает разрешение уведомлений при первой загрузке.
- Проверка вручную: поделиться ссылкой при закрытом/открытом Flow; отклонить/разрешить уведомления;
  начать загрузку, свернуть и выключить экран; вернуться через уведомление, сохранить файл;
  отменить загрузку; повторить с новой ссылкой; проверить ошибку сети.

## Версия 0.4.0

- «Мои загрузки»: история только успешно сохранённых документов, название, размер и дата.
- Открытие во внешнем плеере и отправка через Android Share с временным доступом на чтение.
- Удаление записи не удаляет документ. Если документ перемещён/удалён или доступ отозван,
  приложение показывает сообщение, а не падает.
- История хранится локально. Постоянное разрешение на чтение запрашивается у SAF-провайдера;
  если провайдер его не поддерживает, сохранение всё равно успешно и показывается предупреждение.
- Файлы, сохранённые до 0.4.0, автоматически не импортируются.
- Device-тест `HistoryInstrumentation`: пустая история, повторное чтение, порядок, Unicode,
  дедупликация, удаление записи и восстановление после повреждённого JSON. Использует отдельное
  тестовое хранилище, не реальные документы. Сборка: `assembleDebugAndroidTest`.
- Открытие/отправку реального сохранённого файла и отзыв SAF-доступа проверять отдельно вручную.

## Версия 0.5.0

- «Мои загрузки» разделены на «Видео» и «Музыка» со счётчиками и отдельными пустыми состояниями.
  Категория определяется по MIME, поэтому audio/webm и video/webm не смешиваются.
- Меню файла: «Удалить файл и запись» (с подтверждением необратимого действия) либо
  «Только убрать из истории». DocumentsContract удаляет ровно сохранённый URI; история удаляется
  только после подтверждения успеха провайдером. Отказы и отсутствие прав не скрывают запись.
- Для новых сохранений удерживается доступ на чтение и запись, если предоставлен провайдером.
  Для старых read-only URI предлагается системный выбор того же файла и повторное подтверждение;
  выбор другого URI ничего не удаляет.
- Device-тесты проверяют категории и контракт удаления с подставной операцией (успех, false,
  SecurityException). Реальное удаление SAF-файла тестировать отдельно на ненужном файле.

## Версия 0.6.0

- Для SAF-документов, сопоставимых с MediaStore (ExternalStorageProvider/MediaDocumentsProvider),
  удаление просит подтверждение Android через MediaStore.createDeleteRequest. Запись истории
  убирается только после результата RESULT_OK.
- Для остальных SAF-провайдеров сохраняется удаление через DocumentsContract; системный picker
  повторно запрашивает доступ при необходимости. Отказ пользователя сохраняет запись.
- Android system-confirmation UI requires manual check on the phone for the target document provider.

## Версия 0.7.0

- На Android 10+ новые видео сохраняются напрямую в `Movies/Flow`, музыка в `Music/Flow`
  через MediaStore. Старые Android продолжают использовать системный выбор папки.
- Для созданных Flow записей сохраняется URI MediaStore; удаление вызывает системное
  подтверждение Android для того же URI, и история убирается только после его результата.
- Пункт «Убрать только запись (файл останется)» теперь прямо поясняет своё действие.
- Ранее сохранённые через SAF файлы остаются по старому URI и используют поддержку удаления
  своего провайдера; новый способ не перемещает уже существующие файлы.

## Версия 0.8.0

- В «Мои загрузки» добавлен поиск по названию, отдельно в текущих вкладках «Видео» и «Музыка».
- История новых файлов сохраняет ссылку на YouTube-обложку; карточки показывают превью,
  если оно доступно. Для старых записей без обложки сохраняется компактный вид.
- Загрузка обложки разрешена только с HTTPS-доменов YouTube thumbnails, с ограничением размера
  ответа и тайм-аутом; отсутствие превью не влияет на открытие, отправку или удаление файла.

## Версия 0.9.0

- Прогресс загрузки показывает процент, скорость из вывода yt-dlp и ETA, когда источник их сообщает.
- Отдельно отображается этап подготовки или объединения аудио и видео; неизвестные значения не
  маскируются выдуманными цифрами.
- На экране загрузки есть кнопка отмены с подтверждением; действие также доступно из уведомления.

## Версия 0.2.0

- Проверка обновлений yt-dlp перед первым анализом и затем раз в сутки.
- Один повтор при 403/исчезнувшем формате: обновление и новый анализ, без снижения выбранного качества.
- Компактные RU/EN и вкладки, карточка результата, отдельные технические подробности ошибок.
- Смена ссылки очищает предыдущий результат; сохранение подтверждается отдельным сообщением.
- Ответы YouTube могут по-прежнему ограничивать загрузку. Успешная сборка не подтверждает работу на физическом телефоне.
