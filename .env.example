# Optional absolute path to an installed official yt-dlp executable.
# Otherwise uses .tools/yt-dlp.exe on Windows, .tools/yt-dlp elsewhere.
YTDLP_PATH=
# Absolute path to ffmpeg (defaults to .tools/ffmpeg.exe on Windows).
FFMPEG_PATH=
